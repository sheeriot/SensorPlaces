document.addEventListener('DOMContentLoaded', function () {
    // This file is now largely obsolete as the device move is handled
    // by a server-side redirect via HX-Redirect.
    // Keeping the file for now in case other logic is added in the future.
});
